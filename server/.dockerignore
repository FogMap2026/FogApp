# 빌드 컨텍스트에서 제외한다. 용량 문제만이 아니라 — 시크릿과 업로드된 사진이
# 이미지 레이어에 섞여 들어가지 않게 하는 것이 핵심이다(#86).

# ⛔ 시크릿 — 절대 이미지에 굽지 않는다. 실행 시 볼륨으로 주입한다.
secrets/
*.json
!src/**/*.json

# 인증 사진 — 사용자 데이터다. 영속 볼륨에서 온다.
data/

# 빌드 산출물 — 컨테이너 안에서 새로 빌드한다
build/
.gradle/
bin/
out/

# 로컬 도구
.idea/
*.iml
.vscode/
README.md
