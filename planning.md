# 🗺️ FogApp 개발 로드맵 (Planning)

> 안개 지도 탐험 서비스를 **0에서 완성까지** 끌고 가기 위한 단계별 계획서입니다.
> [README.md](README.md)의 기능 정의와 [CONTRIBUTING.md](CONTRIBUTING.md)의 협업 규칙(브랜치·영역 오너)을 기준으로 작성했습니다.

---

## 🔴 마감: **2026-09-14** — 오늘(9/10) 기준 **D-4**

> 오픈소스 개발자대회 2026 출품작입니다. 1차 심사자료에 **"최소 1개 스토어에 등록 완료된 링크"** 가
> 필요합니다 — 디버그 APK 를 링크로 나눠주는 것으로는 요건을 못 채웁니다. **원스토어 단독**으로 갑니다.

**기능은 사실상 끝났습니다. 남은 4일은 전부 출품 절차입니다.** 신규 기능은 넣지 않습니다.

### 출품 절차 — 순서가 고정돼 있습니다

마일스톤 **[출품 — 원스토어 등록](../../milestone/8)**. 앞 단계가 끝나야 다음이 시작됩니다.

| | 할 일 | 담당 | 없으면 |
|---|---|---|---|
| **①** | [위치정보 신고 필요 여부 — 사전 검토](../../issues/151) | 🟦 박근호 | **등록 화면에서 허위 기재 = 검증 반려** |
| **②** | [개인정보·위치정보 수집 동의 화면](../../issues/152) | 🟩 송진오 · 🟪 김규현 | **명시 반려 사유** |
| **③** | [개인정보처리방침 작성 + 공개 URL](../../issues/153) | 🟦 박근호 · 🟧 송건희 | 등록 화면 입력 항목 |
| **④** | [서버 상시 가동 이전 + 주소 확정](../../issues/154) | 🟧 송건희 | 심사위원이 켰을 때 로그인부터 실패 |
| **⑤** | [스팟 수집 전국 확대 1회](../../issues/155) | 🟧 송건희 | 심사위원 주변에 스팟 0개 |
| **⑥** | [릴리스 키스토어 + 서명](../../issues/156) | 🟧 송건희 | **업로드 자체가 불가** |
| **⑦** | [심사 계정 생성·시딩·동작 확인](../../issues/157) | 🟦 박근호 | 심사위원이 볼 게 없음 |
| **⑧** | [릴리스 빌드 → 스크린샷 → 등록 → 링크 제출](../../issues/158) | 🟧 송건희 · 🟩 송진오 · 🟨 김시진 · 🟦 박근호 | **제출 요건 미충족** |

> 🚨 **④ → 주소 확정 → ⑧ 빌드** 순서를 어기면 심사를 한 번 더 태웁니다.
> `API_BASE_URL` 이 **빌드 타임에 APK 안으로 구워집니다.** 스크린샷도 같습니다.

**①②는 다른 무엇보다 먼저였습니다.** ①은 신고 결과가 나와야 스토어 등록이 되므로 일찍 넣어야 했고(9/10 접수 완료 — **소상공인등 특례**, 서식상 처리기간 「즉시」), ②는 신고 결과와 무관하게 필요했습니다([#160](../../pull/160) 병합).

**함께 처리하는 것** — [#144](../../issues/144)(심사위원 경험)은 ⑤·⑦로 나뉘어 들어갔고, [#141](../../issues/141)(발자취 쿼터)은 ⑦에서 같은 스크립트로 처리합니다. [#146](../../issues/146)(서버 실패 안내)은 ⑧ 빌드 **전에** 들어가야 하는 마지막 앱 변경입니다.

### 🔴 위치정보 — 우리는 신고 대상으로 보입니다

주최측이 준 기준을 코드에 대입한 결과입니다.

> **비신고** — 단말기 내부에서만 활용하고 서버로 전송하지 않는 경우. 또는 **익명·가명 처리**한 경우.
> **신고 대상** — 개인위치정보를 서버로 **전송**하여 활용하는 경우. **DB 저장 여부와 무관하게 전송 자체로 대상.**

앱이 좌표를 서버로 보내는 지점은 **네 곳**이고, 전부 `Authorization: Bearer` 를 달고 갑니다.

| 보내는 곳 | 서버 저장 |
|---|---|
| `POST /api/visits` | `visits.lat`/`lng`/`geom` |
| `POST /api/footprints` | `footprints.lat`/`lng`/`geom` |
| `GET /api/spots/nearby` | 저장 안 함 (전송만) |
| `GET /api/footprints/nearby` | 저장 안 함 (전송만) |

**익명화 우회는 불가능합니다.** 조회 두 건은 여지가 있지만 `visits`·`footprints` 는 **"누가 인증했는가"·"누가 썼는가"가 기능의 본질**이라, 익명화하면 앱이 성립하지 않습니다.

> ⚠️ **법률 판단이 아닙니다.** [#151](../../issues/151) 에서 위치정보지원센터 사전 검토를 받습니다.
> 근거와 출처는 [docs/ONESTORE_RELEASE.md](docs/ONESTORE_RELEASE.md) 1-2 절.

### ⏸ 마감까지 하지 않는 것

- **Phase 6 의 나머지** ([#131](../../issues/131) 여정 · [#133](../../issues/133)·[#134](../../issues/134)·[#135](../../issues/135)) — 임계 경로 밖입니다. 이슈는 열어두되 착수하지 않습니다
  - ✅ **예외: 6-1 축소판(캐릭터 렌더 + heading)은 병합했습니다**([#161](../../pull/161)) — 권한·방침과 무관하고 ⑧ 스크린샷에 쓰입니다. 여정(polyline·`journey_points`)은 마감 후입니다
- **[#125](../../issues/125)** GiST 인덱스 — [#149](../../pull/149) 로 처리됐습니다(전국 확대로 행이 5배가 되어 앞당겼습니다)
- **새 기능** — 기준은 *"출품 절차에 필요한 것은 받고, 새 기능은 받지 않는다"* 입니다([#166](../../pull/166) 에서 PM 이 명확히 함).
  [#146](../../issues/146) 이후로도 출품에 필요한 앱 변경은 받았습니다 — [#161](../../pull/161)(스크린샷용 캐릭터) · [#164](../../pull/164)/[#167](../../pull/167)(실내에서 발자취를 못 남기던 문제) · [#171](../../pull/171)(스팟이 한 점에 뭉쳐 보이던 문제)

---

## 📌 이 문서의 사용법

- 각 **Phase = 하나의 마일스톤**입니다. Phase가 끝날 때마다 `dev`에서 통합 테스트 → 태그(`v0.x`)를 찍습니다.
- 작업 항목은 [CONTRIBUTING.md](CONTRIBUTING.md)의 브랜치 접두어(`feat/api-*`, `feat/map-*` …)와 영역 오너에 매핑됩니다.
- 실제 진행 시에는 각 항목을 **GitHub 이슈로 쪼개서** 담당자에게 할당하세요.

### 담당 영역 약어

| 약어 | 영역 | 담당자 |
|------|------|--------|
| 🟦 **API** | 백엔드·서버 | 박근호 [@PGH0621](https://github.com/PGH0621) |
| 🟩 **UI** | 모바일 프론트 | 송진오 [@oorony](https://github.com/oorony) |
| 🟨 **MAP** | 지도·위치·안개 | 김시진 [@sijin2170](https://github.com/sijin2170) |
| 🟪 **SOC** | 매칭·소셜·발자취 | 김규현 [@k2hop1213](https://github.com/k2hop1213) |
| 🟧 **INF** | 인프라·사진·알림 | 송건희 [@songkh1201](https://github.com/songkh1201) |

### 상태 표기

각 작업 표의 `상태` 열은 다음 뜻입니다.

| 표기 | 의미 |
|------|------|
| ✅ | 완료 — `dev`에 병합됨 |
| 🔄 | 진행 중 — PR이 열려 있고 리뷰/병합 대기 |
| ⏸ | 대기 — 외부 블로커(콘솔 설정·키 발급 등) 때문에 멈춤 |
| ⬜ | 미착수 |

---

## 📊 현재 진행 상황 (2026-09-10 기준 · D-4)

> 이 스냅샷은 `dev` 브랜치 기준입니다. 정확한 최신 상태는 [Issues](../../issues)·[Pull requests](../../pulls)를 보세요.

**Phase 5.5까지 전부 끝났습니다.** 로드맵 55개 항목 중 44개 완료(약 80%).
**기능은 사실상 다 됐고, 남은 것은 출품 절차입니다.**

- 🟢 **스팟 데이터가 전국으로 채워졌습니다** — **12,600 스팟 / 17개 시도**([#155](../../issues/155)).
  소개글은 **2,866건**(서울·부산·제주 중심)입니다. 상세조회가 스팟 1건당 API 1회라 전국을 다 채우려면
  일일 한도로 11일 이상 걸리는데, **소개글은 해금 후에만 보이고 심사위원은 인증을 못 하므로** 지금
  상태로 충분합니다. 지도 마커·이름은 목록 수집만으로 뜹니다.
- 🟢 **발자취 재설계가 완결됐습니다** — [#117](../../issues/117)([#130](../../pull/130)) 병합으로 **길목에 남긴 글귀가 지도에 뜨고 탭하면 읽힙니다.**
- 🟢 **서버를 클라우드로 옮겼습니다** — **`https://fogapp.taild500a1.ts.net`**(네이버 클라우드 + Tailscale Funnel, [#154](../../issues/154)).
  팀원 PC 의존이 사라졌습니다. Ubuntu 24.04 / 2 vCPU / 8GB, `V1~V7` 적용, 12,600 스팟 이관 완료.
  ⏸ **지금은 비용 절감으로 정지해 뒀습니다** — 스토어 등록·심사 직전에 켭니다.
- 🔴 **스토어에 아직 안 올라가 있습니다** — 출품 요건입니다. **원스토어 단독**으로 갑니다([#137](../../pull/137) · [가이드](docs/ONESTORE_RELEASE.md)).

| 트랙 | 상태 |
|------|------|
| **Phase 0~5** | ✅ 완료 — 마일스톤 전부 종료 |
| **Phase 5.5 발자취 재설계** | ✅ **완료** — 5건 전부 병합 |
| **Phase 6 실시간 위치** | ⏸ **마감 후로 연기** — 이슈 [#131](../../issues/131)~[#135](../../issues/135) 는 열어둡니다. 단 **6-1 축소판(캐릭터 렌더)은 병합됨**([#161](../../pull/161)) |
| **Phase 7 완성·출품** | 🔴 **지금 전부** — 7-3·7-6 이 임계 경로 |

열린 이슈 — 출품 절차 ①③⑦⑧([#151](../../issues/151)·[#153](../../issues/153)·[#157](../../issues/157)·[#158](../../issues/158)). ②([#152](../../issues/152))는 [#160](../../pull/160) 병합으로 종료. Phase 6 보류분 4건([#131](../../issues/131)·[#133](../../issues/133)·[#134](../../issues/134)·[#135](../../issues/135)) — 6-1([#131](../../issues/131))은 축소판(캐릭터 렌더)만 병합됐고 여정 시각화는 마감 후로 남음.

### 릴리스

| 태그 | 승격 PR | 내용 |
|------|---------|------|
| `v0.1.0` | [#92](../../pull/92) | Phase 3·4 — 탐험 루프·발자취 |
| `v0.2.0` | [#105](../../pull/105) | Phase 5 매칭·소셜 + 관광공사 API 정상화 |
| `v0.3.0` | [#119](../../pull/119) | 실기기 검증 + 수집·안개 수정 8건 |
| **`v0.4.0`** | [#128](../../pull/128) | Phase 5.5 발자취 재설계 + 서버 배포·CI 정비 |

`main` 은 `v0.4.0`. `dev` 에는 그 뒤로 [#127](../../pull/127)·[#129](../../pull/129)·[#130](../../pull/130)·[#137](../../pull/137) 이 쌓여 있습니다.
**Phase 5.5 가 끝났으니 `v0.5.0` 승격이 가능하고, 스토어에 올릴 빌드는 여기서 나옵니다.**

### Phase 5.5 항목별 — 전부 완료

| 항목 | 결과 |
|------|------|
| 5.5-1 좌표·geom + 트리거 (`V5`) | ✅ [#114](../../issues/114) ([#120](../../pull/120)) |
| 5.5-2 반경 조회 API + 좌표 검증 | ✅ [#115](../../issues/115) ([#121](../../pull/121)) |
| 5.5-3 횟수 제한 + 정복 시 리셋 (`V6`) | ✅ [#116](../../issues/116) ([#122](../../pull/122)) |
| 5.5-4 지도 도형 렌더·줌 임계·탭 팝업 | ✅ [#117](../../issues/117) ([#130](../../pull/130)) — dp 고정 마커, 줌 17 |
| 5.5-5 발자취 남기기 버튼 | ✅ [#118](../../issues/118) ([#126](../../pull/126)) |

**해소된 것**

- [#103](../../issues/103) **수집 배치 미실행** → 2,669 스팟 적재(서울·부산·제주). 오랜 최대 병목이었고,
  이후 [#155](../../issues/155) 로 **전국 12,600 스팟**까지 넓혔습니다
- 수집 배치 버그 6건 → [#107](../../pull/107)·[#109](../../pull/109)·[#111](../../pull/111) — **CI 가 잡을 수 없는 것들**이었습니다
- [#110](../../issues/110) 인증해도 안개가 안 걷히던 문제 → [#112](../../pull/112)
- **APK 재빌드 불가** → [#123](../../pull/123) · **DB 포트 노출** → [#124](../../pull/124)
- **출품 스토어 확정** → [#137](../../pull/137). Google Play 는 개인 계정 테스터 12명 × 14일 요건 때문에 **마감 안에 불가능**했습니다
- [#52](../../issues/52)·[#64](../../issues/64)·[#65](../../issues/65)·[#76](../../issues/76)·[#86](../../issues/86) 등 이전 사이클 항목

**지금의 병목 — 전부 출품 절차입니다**

1. 🔴 **스토어에 안 올라가 있습니다** (7-6). **제출 요건이라 이게 없으면 심사를 못 받습니다.**
   릴리스 서명·서버·동의 화면·방침 게시는 해결됐고, 남은 것은 **위치정보 신고확인증 수령 · 스크린샷 마무리**입니다.

   | | 상태 |
   |---|---|
   | 릴리스 서명 | ✅ **해결** — 키스토어·서명·CI 검증까지([#163](../../pull/163) 병합) |
   | 상시 가동 서버 | ✅ **해결** — 클라우드 이전 완료([#154](../../issues/154)) |
   | **위치정보 신고** | 🔶 **접수 완료** — 사업자등록 9/9 · 「소상공인등의 위치기반서비스사업 신고」 9/10 제출([#151](../../issues/151)). 🔴 **신고확인증 수령 대기** — 서식상 처리기간은 **「즉시」** |
   | 동의 화면 | ✅ **해결** — [#160](../../pull/160) 병합(9/10), 이슈 [#152](../../issues/152) 종료 |
   | 방침 게시 | ✅ **완료** — `https://fogapp.taild500a1.ts.net/privacy-policy.html` (200·`text/html`), 이슈 [#153](../../issues/153) 종료 |
   | 스크린샷 | 🔶 5장 다 촬영·저장소 반영([#185](../../pull/185)) — 5번은 축소본이라 원본 교체, 1번은 지도 컨트롤 겹침이 찍혀 [#187](../../pull/187) 병합 후 재촬영 중 |

   원스토어는 등록비 무료·심사 1~2시간이라 **절차 자체는 하루면 됩니다.**
   절차는 [docs/ONESTORE_RELEASE.md](docs/ONESTORE_RELEASE.md).

2. 🔴 **발표 자료·데모 영상이 착수 0입니다** (7-4). PM 몫이고, 코드가 아니라 **시간이 드는 일**입니다.
   기능이 다 된 지금이 찍을 수 있는 시점입니다.

3. 🟠 **심사용 계정이 실제로 돌아가는지 확인 안 됐습니다.** 심사자료가 형식을 지정했습니다
   (`openapi@…` / `2026openapi!`). 만들어만 두면 안 되고 **로그인 → 지도 → 스팟 인증**까지 봐야 합니다.

   > ⚠️ **발자취 쿼터가 심사에서 걸립니다.** `users.footprint_quota` 기본값이 3이고 **스팟을 정복해야
   > 리셋**됩니다([#116](../../issues/116)). 심사위원이 실제로 걸어서 인증할 리가 없으니 **글 3개 쓰고 나면
   > 이번 사이클 핵심 기능을 더 못 봅니다.** 심사 계정 쿼터를 따로 처리해야 합니다 — 🟦 API.

4. ✅ **CI 가 `flutter test` 를 실행합니다** ([#139](../../issues/139) → [#142](../../pull/142)).
   테스트 8개가 실제로 통과하는 것까지 확인했습니다. 그전까지 앱 PR 의 초록불은
   *"정적 분석 통과"* 였지 *"테스트 통과"* 가 아니었습니다.

5. ✅ **반경 조회가 인덱스를 탑니다** ([#125](../../issues/125) → [#149](../../pull/149), `V7`).
   전국 수집으로 행이 2,669 → 12,600 이 되면서 **미루기 어려워졌고**, 운영 DB 실측으로
   **93.5ms(Seq Scan) → 7.4ms(Index Scan)** 을 확인했습니다.

6. ⏸ **Phase 6 는 마감 후로 미룹니다** — 다만 **이유를 정정합니다.**

   > ✏️ **정정**: 앞서 "6-5 는 스토어 심사에서 민감 권한 소명을 요구해 출시를 늦춘다" 고 적었는데
   > **그건 Google Play 기준이었습니다.** [#137](../../pull/137) 로 원스토어가 됐고, **원스토어에는 Google Play 식
   > 민감 권한 선언·심사 절차가 없습니다** (전체 심사 1~2시간, 별도 퍼미션 제한 가이드 없음).
   > 근거는 [ONESTORE_RELEASE.md 5-2](docs/ONESTORE_RELEASE.md) 참고.

   **실제로 남는 연기 사유는 두 가지입니다.**

   - **임계 경로에 Phase 6 가 없습니다.** 키스토어 → 서버 이전 → 전국 수집 → 릴리스 빌드 →
     원스토어 등록 → 스크린샷 → 발표 자료. 이게 안 되면 심사를 아예 못 받습니다
   - **앱을 건드릴 때마다 릴리스 검증이 재시작되고 동결이 밀립니다**

   이슈별로는 사정이 다릅니다.

   | | 판단 |
   |---|---|
   | [#132](../../issues/132) 6-2 | **불필요해졌습니다** — [#144](../../issues/144) 전국 수집 확대가 사실상 대체합니다. 마감 후 범위 재검토 |
   | [#133](../../issues/133) 6-3 | ⏸ **익명화로 많이 가벼워졌습니다.** 제3자에게 나가는 것이 **익명·30분 텀·스팟 기준**이라 실명 정밀 좌표 제공과 성격이 다릅니다. 다만 **서버는 여전히 사용자와 위치를 연결해 저장**하므로 수집·저장 요건은 남습니다(병목 7번과 같은 건). 연기 사유는 법이 아니라 **임계 경로 밖**이라는 것 |
   | [#134](../../issues/134) 6-4 | 기술적으로는 가능하나 **심사 가치가 없습니다** — 심사위원에게 푸시가 갈 일이 없습니다 |
   | [#135](../../issues/135) 6-5 | ❌ 스토어 때문이 아니라 **`LocationService` 리팩터 리스크** 때문입니다. 그 위치 소스가 내 위치·근접 감지·안개 걷힘·발자취 조회를 전부 먹여줍니다 |
   | [#131](../../issues/131) 6-1 | 🟢 **축소판(캐릭터 렌더 + heading)은 가능합니다** — `FogLocationTracker.headingStream`·`_myLat`/`_myLng`·`NOverlayImage.fromWidget` 이 전부 이미 있습니다. 전체 설계(LocationService 리팩터 + `journey_points` + 여정 API)는 마감 후 |

7. 🔶 **위치정보 신고 — 접수 완료, 신고확인증 수령 대기(9/10 기준).**

   사업자등록(9/9)·위치기반서비스사업 신고 접수(9/10)를 마쳤습니다([#151](../../issues/151)).
   남은 것은 **신고확인증 수령**이고, 그게 스토어 등록의 유일한 외부 대기입니다.
   서식상 처리기간은 **「즉시」** 입니다 — 일반 신고의 5~6일과 다릅니다.
   아래는 이 결정에 이른 배경입니다.

   원스토어는 **개인위치정보를 수집·저장·활용하는 앱**에 방송통신위원회 신고를 요구하고,
   미신고 시 **판매 금지 조치와 형사 제재**가 가능하다고 공지하고 있습니다. 상품 등록 시
   위치정보 수집 여부를 **허위 기재하거나 누락하면 검증 반려**입니다.

   **Phase 6 얘기가 아니라 지금 앱이 이미 해당합니다.** `visits.lat`/`lng`(`V1`·`V4`)와
   `footprints.lat`/`lng`(`V5`)를 계정에 묶어 서버 DB 에 저장하고 있습니다.

   검토 결과 신고 대상으로 판단해, 사업자등록 후 **「소상공인등의 위치기반서비스사업 신고」**(위치정보법 §9의2)로
   접수했습니다 — 팀이 5명이라 1인창조기업(상시근로자 0명)이 아니라 소상공인(정보통신업 5명 미만) 쪽을 골랐습니다.
   절차와 출처는 [ONESTORE_RELEASE.md 1-2](docs/ONESTORE_RELEASE.md).

8. 🟡 **iOS 컴파일 검증이 사라졌습니다** ([#137](../../pull/137)). 출품 범위 밖이라 맞는 결정이지만,
   코드가 iOS 에서 빌드되지 않는 상태로 조용히 드리프트합니다. **복원 비용은 시간이 갈수록 커집니다.**

### 🍎 Flutter 버전 — 3.44.9로 맞추세요

`app/android`(Gradle 9.1·AGP 9.0)와 `app/ios`(implicit engine API) 스캐폴딩이 이 버전 기준이라,
**더 낮은 버전에서는 네이티브 빌드가 아예 되지 않습니다.** `flutter analyze`만 통과해서 오래 드러나지 않았던 문제입니다.

---

## 🚨 스택(Stacked) PR 병합 규칙 — squash 금지

PR이 `dev`가 아니라 **다른 PR의 브랜치를 base로** 올라오면(스택 PR), **squash 병합이 위쪽 PR을 전부 충돌시킵니다.**
Phase 2 지도 PR 5건에서 실제로 발생한 문제입니다.

### 왜 깨지는가

```
feat/map-base :  A ─ B ─ C          ← feat/ui-map-screen 이 이 커밋들 위에 쌓임
                       │
      squash 병합 ─────┴──► dev :  [ABC]   ← A·B·C 가 하나의 새 커밋으로 압축

결과: dev 의 [ABC] 와 feat/ui-map-screen 의 A·B·C 는
      내용은 같지만 커밋 ID가 다르다
      → git 이 "양쪽이 각각 수정했다"고 판단 → 충돌
```

같은 코드가 서로 다른 커밋으로 존재하게 되어 공통 조상을 찾지 못하는 것입니다.
**내용이 충돌한 게 아니라 이력이 끊긴 것**이라, 코드를 아무리 봐도 원인이 안 보입니다.

### 규칙

| 상황 | 소스 브랜치가 병합 후에도 살아 있는가 | 병합 방식 |
|------|--------------------------------------|-----------|
| `feat/* → dev` | ❌ 병합 후 삭제 | **Squash and merge** |
| **`dev → main`** (릴리스 승격) | ✅ `dev`는 계속 쓴다 | ⚠️ **Create a merge commit** |
| **스택 PR** (base가 다른 feature 브랜치) | ✅ 위에 PR이 매달려 있다 | ⚠️ **Create a merge commit** |
| **되돌리기 병합** (`main → dev` 등 이력 편입 목적) | ✅ | ⚠️ **Create a merge commit** |

> 판단 기준은 하나입니다. **squash는 소스 브랜치를 버릴 때만.**
> 병합 후에도 그 브랜치를 계속 쓴다면 squash가 조상 관계를 끊고 다음 병합에서 반드시 충돌합니다.
> base가 무엇이냐가 아니라 **소스 브랜치의 수명**으로 판단하세요.
> 특히 `dev → main`은 `dev`가 장수 브랜치라, squash하면 **매 릴리스마다** 같은 충돌이 반복됩니다.

### 이미 깨졌다면

당황할 필요 없습니다. 위쪽 브랜치가 아래쪽 내용을 이미 포함하고 있으므로, **자기 브랜치 버전을 채택**하면 됩니다.

```bash
git checkout feat/내-브랜치
git fetch origin
git merge origin/dev            # 또는 base 브랜치

# 충돌 파일은 대개 내 브랜치가 상위집합이다 → 내 버전 채택
git checkout --ours <충돌파일>
git add <충돌파일>

# ✅ 채택 전 반드시 확인: base 쪽에만 있는 내용이 사라지지 않는지
git diff --cached origin/dev --diff-filter=D --name-only   # 출력 없어야 정상

git commit
git push
```

`--ours`는 되돌릴 수 없는 선택이 아니므로, 커밋 전에 위 `--diff-filter=D` 확인만 꼭 하세요.
자세한 배경은 [CONTRIBUTING.md](CONTRIBUTING.md) 5번 "스택 PR 병합" 참고.

### 애초에 스택을 만들지 않으려면

가능하면 **각 PR의 base를 `dev`로** 두고, 앞 작업이 필요하면 그때그때 `git merge origin/dev`로 따라가는 편이 낫습니다.
스택은 리뷰 단위를 작게 나눠주는 장점이 있지만, 병합 방식을 한 번만 틀려도 위쪽 전부가 막힙니다.

---

## 🧭 전체 그림 — 8단계

```
Phase 0  기반 다지기        프로젝트 뼈대 · 개발환경 · 협업 셋업
   ↓
Phase 1  인증 & 데이터 토대  로그인 · DB(PostGIS) · 관광공사 OpenAPI 수집
   ↓
Phase 2  지도 & 안개 코어    지도 렌더 · 안개 오버레이 · 스팟 배치 · GPS 추적
   ↓
Phase 3  탐험의 핵심 루프    근접 감지 · 사진 인증 · 안개 해제 · 정복률   ★MVP★
   ↓
Phase 4  발자취 시스템       기록 작성/조회 · 좋아요·공감
   ↓
Phase 5  성향 매칭          성향 테스트 · 매칭 알고리즘 · 동행 추천
   ↓
Phase 6  실시간 위치 공유    캐릭터 지도 · 30분 갱신 · 푸시 알림
   ↓
Phase 7  완성 & 출품         QA · 성능 · 배포 · 발표 자료
```

> **★MVP 기준선★**: Phase 3까지 완료되면 "안개를 걷으며 탐험한다"는 **핵심 가치가 동작**합니다.
> 시간이 부족하면 Phase 3을 데모 가능한 최소 완성본으로 잡고, 4~6은 우선순위대로 붙입니다.

### 🟪 병렬 소셜 트랙 (SOC · 김규현)

발자취·매칭·성향은 대부분 **지도/안개와 무관**하고, 스키마(`footprints`·`matches`·`users.personality_*`)가 [#1](../../issues/1)에서 이미 준비돼 있습니다.
그래서 SOC 작업은 탐험 코어(MAP/UI/API)와 **별도 트랙으로 Phase 1부터 병렬 진행**합니다.

```
탐험 코어 트랙 :  Phase 1 데이터 ─ Phase 2 지도 ─ Phase 3 탐험루프(MVP) ─┐
                                                                          ├─► Phase 4·5 통합·완성
소셜 트랙(SOC) :  성향 설계 ─ 성향 테스트·발자취 API ─ 매칭 알고리즘 ─────┘
                  (Phase 1)    (Phase 2)               (Phase 3)
```

**원칙**: 지도 의존성이 없는 **로직·API·설계**는 앞당기고(Phase 1~3), 지도 통합이 필요한 **UI**만 Phase 4~5에 둔다.
→ 김규현도 Phase 1부터 끊김 없이 작업하고, Phase 4·5에서는 "UI만 붙이면 완성"인 상태가 된다.

---

## Phase 0 — 기반 다지기 (Foundation)

**목표**: 5명이 충돌 없이 동시에 개발을 시작할 수 있는 상태를 만든다.

| # | 작업 | 영역 | 상태 | 결과물 |
|---|------|------|------|--------|
| 0-1 | `dev` 브랜치 생성 + `main`/`dev` 브랜치 보호 규칙 설정 | 🟦 API(PM) | ✅ | 양쪽 다 PR 필수 + 승인 1명 이상 적용됨 |
| 0-2 | 이슈/PR 템플릿, 라벨(feature/bug/영역별) 생성 | 🟦 API(PM) | ✅ | [.github/ISSUE_TEMPLATE](.github/ISSUE_TEMPLATE), 라벨 10종 |
| 0-3 | Flutter 앱 스캐폴딩(`app/`) + 폴더 구조·라우팅·상태관리 세팅 | 🟩 UI | ✅ | `app/` (Flutter 3.44.9 · Riverpod) |
| 0-4 | Spring Boot 서버 스캐폴딩(`server/`) + 헬스체크 API | 🟦 API | ✅ | `GET /api/health` |
| 0-5 | PostgreSQL + PostGIS 로컬 실행(docker-compose) | 🟧 INF | ✅ | [docker-compose.yml](docker-compose.yml) |
| 0-6 | Firebase 프로젝트 생성(Auth/FCM) + 설정 커밋 정책 | 🟧 INF | ✅ | [#2](../../issues/2) 종료. Storage는 무료 요금제 제약으로 미채택 — 사진은 서버 보관([#68](../../pull/68)) |
| 0-7 | `.env.example`, `.gitignore`, 시크릿 관리 규칙 문서화 | 🟧 INF | ✅ | [ENV_GUIDE.md](docs/ENV_GUIDE.md) |
| 0-8 | GitHub Actions CI 뼈대(빌드·린트) | 🟧 INF | ✅ | 변경 영역 감지로 `app`/`server` 잡 분리 |

**완료 기준(DoD)**: 앱이 빈 화면이라도 실행되고, 서버 헬스체크가 200을 반환하며, 모두 로컬 DB에 연결된다.
→ **충족.** 실기기(에뮬레이터)에서 로그인 → 안개 지도 → 내 위치 표시까지 확인했습니다.

---

## Phase 1 — 인증 & 데이터 토대 (Auth & Data)

**목표**: 사용자가 로그인할 수 있고, 지도에 뿌릴 관광 스팟 데이터가 DB에 쌓인다.

| # | 작업 | 영역 | 상태 | 이슈 / PR |
|---|------|------|------|-----------|
| 1-1 | 소셜 로그인(Firebase Auth) + 앱 로그인/온보딩 화면 | 🟩 UI / 🟧 INF | ✅ 화면 / ⏸ 콘솔 | [#3](../../issues/3) ✅ ([PR #12](../../pull/12)) · [#2](../../issues/2) ⏸ |
| 1-2 | 서버 JWT 검증 미들웨어 · 사용자 프로필 API | 🟦 API | ✅ 코드 / ⏸ 키 | [#4](../../issues/4) ([PR #20](../../pull/20)) — 서비스 계정 키 대기 |
| 1-3 | DB 스키마 설계(users, spots, visits, footprints, matches …) | 🟦 API | ✅ | [#1](../../issues/1) ([PR #8](../../pull/8)) |
| 1-4 | **관광공사 OpenAPI 연동** — 지역기반 스팟 수집·정제·캐싱 배치 | 🟦 API | ✅ | [#5](../../issues/5) ([PR #21](../../pull/21)) |
| 1-5 | 스팟 좌표를 PostGIS `geometry`로 저장 + 공간 인덱스 | 🟨 MAP / 🟦 API | ✅ | [#6](../../issues/6) ([PR #14](../../pull/14)) |
| 1-6 | 스팟 조회 API(지역별/반경별) | 🟦 API | ✅ | [#7](../../issues/7) ([PR #22](../../pull/22)) |
| 1-7 | 🟪 **여행 성향 테스트 설계**(설문 문항·성향 축·점수 모델) — 지도 의존성 없음 | 🟪 SOC | ✅ | [#15](../../issues/15) ([PR #16](../../pull/16)) |
| 1-8 | 🟪 **발자취·매칭 API 뼈대**(CRUD, 스키마 기반) | 🟪 SOC / 🟦 API | ✅ | [#18](../../issues/18) ([PR #19](../../pull/19)) |

> 🟪 소셜 트랙 시작: 1-7·1-8은 **#1 스키마에만 의존**(완료)하므로 인증·지도 작업과 무관하게 병렬 착수 가능.

**완료 기준(DoD)**: 로그인 후 서버에서 "특정 지역 스팟 목록"을 좌표와 함께 받아올 수 있다.
소셜 트랙: 성향 점수 모델이 확정되고, 발자취·매칭 API 뼈대가 동작한다.

> **현재**: 8개 항목 모두 완료되어 이슈도 전부 닫혔고, [#25](../../pull/25)로 `main`까지 승격됐습니다.

---

## Phase 2 — 지도 & 안개 코어 (Map & Fog Core)

**목표**: 지도 위에 회색 안개가 덮이고, 스팟이 숨겨진 상태로 배치된다.

| # | 작업 | 영역 | 상태 | 이슈 / PR |
|---|------|------|------|-----------|
| 2-1 | Naver Maps SDK 연동 + 기본 지도 화면 | 🟨 MAP | ✅ | [#26](../../issues/26) ([PR #34](../../pull/34) → `dev` 병합 완료) |
| 2-2 | **안개 오버레이 렌더링**(전국 회색 레이어) | 🟨 MAP | ✅ | [#27](../../issues/27) ([PR #39](../../pull/39)) |
| 2-3 | Phase 1의 스팟 API를 지도에 로드(숨김 상태 마커) | 🟨 MAP / 🟩 UI | ✅ | [#28](../../issues/28) ([PR #40](../../pull/40)) |
| 2-4 | 실시간 GPS 추적 + 내 위치 표시 | 🟨 MAP | ✅ | [#29](../../issues/29) ([PR #41](../../pull/41)) |
| 2-5 | 지도 화면 UI/UX(줌·이동·현재 지역 표시) | 🟩 UI | ✅ | [#30](../../issues/30) ([PR #35](../../pull/35)) |

> 📌 2-2·2-4는 코드가 완성돼 상위 브랜치에 병합됐지만, 그 브랜치가 아직 `dev`에 닿지 못해 이슈는 열려 있습니다.
> [#35](../../pull/35) → [#40](../../pull/40) 순서로 `dev`까지 올라가면 [#27](../../issues/27)·[#28](../../issues/28)·[#29](../../issues/29)·[#30](../../issues/30)이 한꺼번에 닫힙니다.
| 2-6 | 🟪 **성향 테스트 화면 + 결과 저장**(`users.personality_*`) | 🟪 SOC / 🟩 UI | ✅ | [#31](../../issues/31) ([PR #33](../../pull/33)) + [#32](../../issues/32) 저장 API ([PR #36](../../pull/36)) |
| 2-7 | 🟪 **발자취 작성 API 완성 + 좋아요·공감 API** | 🟪 SOC | ✅ | [#23](../../issues/23) ([PR #24](../../pull/24)) |

> 🟪 소셜 트랙: 2-6은 로그인(1-1)에만 의존, 2-7은 발자취 뼈대(1-8)에 의존 — 지도 코어와 병렬.

### ⚠️ MAP 스택 PR 병합 순서 — 남은 2건

2-1 ~ 2-5의 PR 5개는 각자 `dev`가 아니라 **바로 앞 PR의 브랜치를 base로** 올라와 있었습니다.
[#34](../../pull/34)·[#39](../../pull/39)·[#41](../../pull/41)은 병합됐고, 남은 순서는 다음과 같습니다.

```
dev ← #35 feat/ui-map-screen ← #40 feat/map-spot-load
      (1순위)                    (2순위)
```

> 🚨 **두 PR 모두 반드시 "Create a merge commit"으로 병합하세요.**
> [#34](../../pull/34)·[#39](../../pull/39)를 squash로 병합한 탓에 지금 두 PR이 충돌 상태입니다.
> [#35](../../pull/35)를 또 squash하면 [#40](../../pull/40)에 같은 충돌이 다시 생깁니다.
> 이유와 해소법은 위 [스택 PR 병합 규칙](#-스택-stacked-pr-병합-규칙--squash-금지) 참고.

**완료 기준(DoD)**: 앱을 켜면 안개 덮인 지도가 뜨고, 내 위치가 지도 위에 실시간으로 표시된다.
소셜 트랙: 성향 테스트를 마치면 결과가 저장되고, 발자취·좋아요 API가 완성된다.
→ **충족 ✅** — 스택 PR 5건 전부 병합 완료.

---

## Phase 3 — 탐험의 핵심 루프 (Explore Loop) ★MVP★

**목표**: **도달 → 인증 → 안개 해제 → 정복률 상승**의 핵심 게임 루프를 완성한다.

> 📢 **이슈 등록 완료 (2026-08-07)** — 마일스톤 `Phase 3 — 탐험의 핵심 루프 (MVP)`

| # | 작업 | 영역 | 담당 | 이슈 | 브랜치 예시 |
|---|------|------|------|------|-------------|
| 3-1 | 스팟 반경 **geofencing**(근접 감지) | 🟨 MAP | 김시진 | [#45](../../issues/45) ✅ | `feat/map-geofence` |
| 3-2 | 근접 시 **인앱 알림**(포그라운드) | 🟨 MAP | 김시진 | [#46](../../issues/46) ✅ | `feat/map-proximity-alert` |
| 3-3 | 현장 사진 촬영 → **방문 인증** UI | 🟩 UI | 송진오 | [#47](../../issues/47) ✅ | `feat/ui-visit-verify` |
| 3-4 | 인증 사진 업로드 파이프라인(**서버 보관**) + 인증 API | 🟧 INF / 🟦 API | 송건희·박근호 | [#48](../../issues/48) ✅ | `feat/api-visit`, `feat/infra-photo-upload` |
| 3-5 | 인증 성공 시 해당 반경 **안개 걷힘** 애니메이션 | 🟨 MAP | 김시진 | [#49](../../issues/49) ✅ | `feat/map-fog-clear` |
| 3-6 | 스팟 정보(명칭·주소·소개) 해금 표시 | 🟩 UI | 송진오 | [#50](../../issues/50) ✅ | `feat/ui-spot-detail` |
| 3-7 | 지역별 **정복률(%) 계산·표시** | 🟦 API / 🟩 UI | 박근호·송진오 | [#51](../../issues/51) ✅ | `feat/api-conquest-rate`, `feat/ui-conquest` |
| 3-8 | 🟪 **매칭 알고리즘**(성향 유사도 + 지역/거리 필터) 선행 구현 | 🟪 SOC | 김규현 | [#37](../../issues/37) ✅ | ([PR #38](../../pull/38)) |

> 🟪 소셜 트랙: 3-8은 성향 데이터(2-6)에만 의존 — 탐험 루프와 무관하게 병렬. Phase 5에서 화면만 붙이면 됨. **완료됨.**

### 🔗 Phase 3 내부 의존 순서

7개가 모두 병렬은 아닙니다. **3-4(인증 API)가 허브**라 여기가 늦어지면 3-5·3-6·3-7이 전부 대기합니다.

```
3-1 근접감지 ─┬─► 3-2 근접알림
              └─► 3-3 인증 UI ─┐
                               ├─► 3-4 인증 API ─┬─► 3-5 안개 걷힘
                               │   (허브)         ├─► 3-6 정보 해금
                               │                 └─► 3-7 정복률 ─► ★MVP 완성★
                       #48 사진 저장(서버 보관) ───┘
```

**착수 우선순위**

1. **[#48](../../issues/48) 인증 API** — 가장 먼저. 여기가 막히면 절반이 멈춥니다. `visits` 테이블은 [#1](../../issues/1)에서 이미 준비돼 스키마 작업이 필요 없습니다.
2. **[#45](../../issues/45) 근접 감지** — 3-2·3-3의 입력. Phase 2 PR([#40](../../pull/40)) 병합 후 착수.
3. 나머지는 위 두 개가 열리는 대로.

**팀 결정 사항**

- ✅ **인증 반경 = 100m** — 앱 `SpotGeofenceController.enterRadiusMeters` 와 서버 `visit.radius-meters` 가 같은 값입니다.
  ⚠️ 한쪽만 바꾸면 "화면엔 인증 가능인데 서버가 거부"하는 버그가 됩니다. **반드시 양쪽을 함께** 바꾸세요.
  (서버는 `VISIT_RADIUS_METERS` 환경 변수로도 덮이므로 배포 환경 변경 시 특히 주의)
- ✅ **정복률 지역 단위 = 시/군/구** — 시/도는 분모가 너무 커서 정복률이 거의 오르지 않습니다.
  시/군/구여야 "경주 15%" 같은 즉각적인 성취감이 나옵니다. `GET /api/conquest` 가 이 단위로 집계합니다.
- ✅ **3-2 알림 범위 = 포그라운드 인앱 알림만** — 백그라운드 로컬 알림·FCM 푸시는 **Phase 6(6-4)으로 이관**했습니다.
  백그라운드 위치는 iOS `location always` 심사·Android 백그라운드 위치 선언(시연 영상 + 수 주 심사)·상시 포그라운드 서비스 알림·배터리 소모가 한꺼번에 붙는데,
  그 비용이 **Phase 6의 6-1(캐릭터 실시간 위치)·6-4(FCM)와 그대로 중복**됩니다. 위치 소스는 그때 한 번만 만드는 게 맞습니다.
  `SpotGeofenceController`는 위치 소스와 분리돼 있어 판정 로직은 그대로 재사용되므로, 나중에 백그라운드로 확장하는 길이 막히지 않습니다.

> ⚠️ **선행 블로커**: 3-1·3-3·3-5·3-6은 Phase 2 지도 코드가 `dev`에 있어야 합니다 → [#35](../../pull/35)·[#40](../../pull/40) 병합 필요.
> [#48](../../issues/48)의 Storage 부분은 [#2](../../issues/2) Firebase 콘솔 설정이 선행입니다.
> 다만 **[#48](../../issues/48)의 서버 API 부분은 지금 바로 시작할 수 있습니다.**

**완료 기준(DoD)**: 스팟에 도달해 사진을 찍으면 안개가 걷히고, 지역 정복률이 올라간다. → **데모 가능한 MVP 완성**
소셜 트랙: 성향 유사도 기반 매칭 후보 산출 로직이 API로 동작한다. ✅

---

## Phase 4 — 발자취 통합·완성 (Footprints Integration)

**목표**: Phase 2에서 만든 발자취·좋아요 API를 **지도·UI에 통합**해 완성한다.
(작성/좋아요 API는 2-7에서 선행 완료 → 여기서는 지도 연결·화면 중심)

| # | 작업 | 영역 | 상태 | 브랜치 예시 |
|---|------|------|------|-------------|
| 4-1 | 지도에서 스팟 탭 → 발자취 작성 플로우 연결 | 🟪 SOC / 🟩 UI | ✅ [#70](../../issues/70) | `feat/social-footprint-flow` |
| 4-2 | 지도/스팟에서 발자취 카드 조회 UI | 🟩 UI / 🟪 SOC | ✅ [#71](../../issues/71) | `feat/ui-footprint-card` |
| 4-3 | 좋아요·공감 UI 연동(2-7 API 사용) | 🟪 SOC / 🟩 UI | ✅ [#72](../../issues/72) | `feat/ui-reaction` |
| 4-4 | 내 발자취 모아보기(프로필) | 🟩 UI | ✅ [#73](../../issues/73) | `feat/ui-profile-footprints` |

> 서버 쪽 `/api/footprints` CRUD·좋아요는 2-7에서 **이미 완성**되어 있습니다. 여기서는 화면만 붙이면 됩니다.

> 지도(Phase 2·3)가 있어야 "장소에 남기고 지도에서 보는" 경험이 완성되므로, UI 통합은 여기에 둔다.

**완료 기준(DoD)**: 방문한 스팟에 발자취를 남기고, 지도에서 다른 사람 발자취를 보고 좋아요를 누를 수 있다.

---

## Phase 5 — 매칭 화면·완성 (Matching Completion)

**목표**: 선행 구현된 **성향 테스트(1-7·2-6)·매칭 알고리즘(3-8)**을 화면으로 완성한다.

| # | 작업 | 영역 | 상태 | 브랜치 예시 |
|---|------|------|------|-------------|
| 5-1 | 매칭 결과·동행 추천 화면 | 🟩 UI / 🟪 SOC | ✅ [#93](../../issues/93) | `feat/ui-match-result` |
| 5-2 | 동행 요청·수락 플로우(`matches.status`) | 🟪 SOC | ✅ [#94](../../issues/94) | `feat/social-match-request` |
| 5-3 | 성향 결과 프로필 노출 + 성향 맞춤 발자취 필터 | 🟪 SOC / 🟩 UI | ✅ [#95](../../issues/95) — 발자취 필터는 범위에서 분리 | `feat/social-personality-profile` |

> 설계(1-7)·저장(2-6)·알고리즘(3-8)이 이미 끝나 있어, Phase 5는 **화면·상호작용 완성**에 집중.
>
> ⚠️ **`MatchResponse` 에 상대방 닉네임이 없습니다** — `requesterId`·`addresseeId` 만 내려줍니다.
> [#71](../../issues/71)과 같은 문제이고 그때 만든 `UserSummary` 를 재사용하면 됩니다.
> 5-2 착수 전 🟦 API(@PGH0621)에게 알려주세요. **5-3의 성향 맞춤 발자취 필터도 서버 작업이 필요**합니다.
> 5-2의 서버 측(`POST /api/matches`, `PATCH /api/matches/{id}/status`)은 [PR #19](../../pull/19)·[#38](../../pull/38)에서 이미 완성됐습니다.

**완료 기준(DoD)**: 성향이 비슷한 동행을 추천받고, 요청·수락으로 매칭이 성사된다.

---

## Phase 5.5 — 발자취 재설계 (Footprint Messages)

**목표**: 발자취를 **스팟 리뷰**에서 **길목마다 남기는 짧은 글귀**로 바꾼다(엘든링 메시지 방식).

> 📄 **설계 확정 문서: [docs/footprint-redesign.md](docs/footprint-redesign.md)** — 구현은 이 문서를 기준으로 한다.
> 반경·횟수 값을 바꾸려면 문서부터 고칠 것.

| # | 작업 | 영역 | 상태 | 브랜치 예시 |
|---|------|------|------|-------------|
| 5.5-1 | `footprints`에 좌표·geom 추가 + 트리거 (기존 행 보정) | 🟦 API | ✅ [#114](../../issues/114) ([#120](../../pull/120)) | `feat/api-footprint-geo` |
| 5.5-2 | 반경 조회 API + 작성 시 좌표 검증 | 🟦 API | ✅ [#115](../../issues/115) ([#121](../../pull/121)) | `feat/api-footprint-nearby` |
| 5.5-3 | 발자취 횟수 제한 + 방문 인증 시 리셋 | 🟦 API | ✅ [#116](../../issues/116) ([#122](../../pull/122)) | `feat/api-footprint-quota` |
| 5.5-4 | 지도에 발자취 도형 렌더 + 줌 임계 숨김 + 탭 팝업 | 🟨 MAP / 🟩 UI | ✅ [#117](../../issues/117) ([#130](../../pull/130)) | `feat/map-footprint-marker-render` |
| 5.5-5 | 발자취 남기기 버튼(10m 검증·잔여 횟수 표시) | 🟪 SOC / 🟩 UI | ✅ [#118](../../issues/118) ([#126](../../pull/126)) | `feat/ui-footprint-drop` |

**핵심 값** — 작성 **10m** · 이동 중 조회 **50m** · 스팟 안 조회 **150m**(안개 걷힘 반경과 동일) · 인증 **100m**.

> 🔑 **도배 방지가 이 설계의 중심입니다.** 발자취 작성 횟수를 제한하고 **스팟을 정복하면 리셋**합니다 —
> "글을 더 남기고 싶으면 더 걸어라"가 되어 도배 방지와 탐험 유도가 한 장치로 해결됩니다.

**의존 순서**: `5.5-1` → `5.5-2`·`5.5-3` → `5.5-4`·`5.5-5`. 좌표가 없으면 나머지가 전부 막힙니다.

**완료 기준(DoD)**: 걷다가 지도에서 남의 글귀를 발견해 읽고, 내 자리에 하나 남길 수 있다.

---

## Phase 6 — 실시간 위치 공유 (Live Sharing)

**목표**: 지도 위 캐릭터로 여정을 공유하고, 주변 스팟을 주기적으로 갱신한다.

> 📌 **"실시간" 은 이름뿐이고 실제 설계는 세 겹으로 느슨합니다** — **익명 · 30분 텀 · 스팟 기준**
> ([README](README.md) 기능 5의 *"저 여행자는 지금 어떤 곳 근처에"*). 누구인지 드러내지 않고, 초 단위로
> 따라다니지 않으며, 좌표가 아니라 "어떤 스팟 근처" 만 전달합니다.
>
> **타협이 아니라 기획된 값입니다.** 실명·실시간·정밀 좌표는 감시에 가깝고 여행자에게 필요한 정보도
> 아닙니다. **셋 중 하나만 풀어도 성격이 완전히 달라지므로**, 바꾸려면 이 문단부터 고치세요.

| # | 작업 | 영역 | 상태 | 브랜치 예시 |
|---|------|------|------|-------------|
> ⏸ **마감(9/14) 후로 연기했습니다.** 이슈는 열어두되 착수하지 않습니다 — 임계 경로가 출품 절차이고,
> 앱을 건드릴 때마다 릴리스 검증이 재시작되기 때문입니다.
>
> ✏️ 앞서 적었던 *"6-5 는 스토어 심사에서 민감 권한 소명을 요구한다"* 는 **Google Play 기준이었고
> 원스토어에는 그 절차가 없습니다** — 위 병목 6번 참고. 대신 **[#133](../../issues/133) 은 개인위치정보의
> 제3자 제공**이라 위치정보법 확인이 필요합니다(병목 7번).
>
> 📌 **6-3 은 실시간도, 실명도 아닙니다.** 원본 기획은 **익명 · 30분 텀 · "어떤 스팟 근처인지"** 입니다.
> 매칭과도 무관합니다 — 주변 사용자가 **익명 여행자**로 뜹니다. 처음에 이슈를 "실시간 위치 동기화 +
> 수락된 동행에게만" 으로 써둔 것은 잘못이라 [#133](../../issues/133) 을 다시 썼습니다.

| # | 작업 | 영역 | 상태 | 브랜치 예시 |
|---|------|------|------|-------------|
| 6-1 | 캐릭터 기반 내 위치 렌더 + heading | 🟨 MAP / 🟩 UI | ✅ [#161](../../pull/161) | `feat/map-character` |
| 6-1 | 여정(경로) 시각화 + `journey_points` 저장 | 🟨 MAP / 🟦 API | ⏸ [#131](../../issues/131) | `feat/map-journey` |
| 6-2 | 30분 단위 주변 스팟 데이터 갱신 | 🟦 API | ✅ [#155](../../issues/155) 로 대체 — [#132](../../issues/132) CLOSED | `feat/api-live-refresh` |
| 6-3 | **주변 여행자 익명 표시 — 30분 텀·스팟 기준** | 🟪 SOC | ⏸ [#133](../../issues/133) | `feat/social-traveler-presence` |
| 6-4 | 푸시 알림 통합(FCM) — 근접·조우 이벤트 | 🟧 INF | ⏸ [#134](../../issues/134) | `feat/infra-fcm` |
| 6-5 | **백그라운드 위치 소스 + 로컬 알림** (3-2에서 이관) | 🟨 MAP / 🟧 INF | 🔶 [#166](../../pull/166) 승인·Draft — 병합 여부 미정 | `feat/map-background-location` |

> 📌 **6-5는 Phase 3의 3-2에서 넘어온 항목입니다.** 앱이 꺼져 있어도 근접 알림을 받으려면
> 위치 소스를 `MapScreen` 생명주기에서 떼어내야 하는데, 그 작업이 6-1(캐릭터 실시간 위치)과 동일합니다.
> **6-1·6-4·6-5는 한 묶음으로 설계하세요** — 따로 하면 위치 소스를 두 번 만들게 됩니다.
>
> 착수 전 확인할 것:
> - iOS `location always` 권한 + App Store 심사 사유 소명
> - Android `ACCESS_BACKGROUND_LOCATION` + Play Console 선언(시연 영상, 심사 수 주)
> - 포그라운드 서비스 상시 알림이 UX에 주는 영향
> - `flutter_local_notifications` 의존성 추가

**완료 기준(DoD)**: 지도에서 캐릭터로 표현된 사용자의 위치가 주기적으로 갱신·공유된다.

---

## Phase 7 — 완성 & 출품 (Polish & Release)

**목표**: 안정화하고 대회 제출·발표 준비를 마친다.

| # | 작업 | 영역 | 상태 |
|---|------|------|------|
| 7-1 | 통합 QA·버그 픽스(`fix/*`) | 전원 | ⬜ |
| 7-2 | 성능 최적화(지도 렌더·쿼리·이미지) | 🟨 MAP / 🟦 API / 🟧 INF | ⬜ |
| 7-3 | 배포 파이프라인 완성(CI/CD) + 릴리스 빌드 | 🟧 INF | 🔶 컨테이너화 ✅ · 서버 배포 ✅ · **스토어 릴리스 빌드 미완** |
| 7-4 | 시연 시나리오·발표 자료·데모 영상 | 🟦 API(PM) 주도, 전원 | 🔴 **착수 0 — 마감 필수 제출물** |
| 7-5 | 라이선스 확정 + README 최종 정리 | 🟦 API(PM) | ⬜ `LICENSE` 파일 없음 |
| 7-6 | **원스토어 출시** — 출품 요건(스토어 등록 링크). Google Play·iOS 는 범위 밖 | 🟧 INF | ⬜ **[가이드](docs/ONESTORE_RELEASE.md)** |
| 7-7 | **인증 사진 저장 경로를 영속 볼륨에 마운트** | 🟧 INF | ✅ ([#86](../../issues/86)) |

**완료 기준(DoD)**: `main`에 안정 버전이 있고, 시연·발표 준비가 끝난다. 🎉

### ✅ 7-7 — 인증 사진 볼륨 (완료)

사진 보관이 Firebase Storage에서 **서버 로컬 디스크**로 바뀌었습니다([#68](../../pull/68)).
컨테이너를 그냥 띄우면 **재시작할 때마다 인증 사진이 전부 사라지고** `visits.photo_url` 은 깨진 링크만 남습니다.

[#86](../../issues/86)에서 `docker-compose.yml` 의 `server` 서비스에 반영했습니다.

```yaml
volumes:
  - fogapp_photos:/app/data/visit-photos
environment:
  VISIT_PHOTO_STORAGE_PATH: /app/data/visit-photos   # ⚠️ 위 마운트 지점과 같아야 한다
```

> ⚠️ **두 값이 어긋나면 조용히 깨집니다.** 서버는 정상 동작하고 업로드도 성공하는데, 파일이 볼륨 **밖**에 쌓여 재시작 순간 전부 사라집니다. CI(`Server Container` 잡)가 매번 대조합니다.

**남은 것 — 백업.** 볼륨은 생겼지만 백업 구성은 없습니다. 배포처가 정해질 때 DB와 함께 정합니다. 자세한 내용은 [docs/STORAGE_SETUP.md](docs/STORAGE_SETUP.md) 5장 참고.

### 📦 7-6 — 원스토어 출시 (Google Play·iOS 는 범위 밖)

**2026-09-08 결정: 원스토어 단독으로 갑니다.** 대회 1차 심사자료가 *"구글 스토어, 애플 스토어,
**원스토어** 등 최소 1개 스토어에 등록 완료된 링크"* 를 요구합니다. **"최소 1개"** 라 원스토어
하나로 충족됩니다.

> 📄 **절차·체크리스트는 [docs/ONESTORE_RELEASE.md](docs/ONESTORE_RELEASE.md) 에 있습니다.**

**디버그 APK 를 링크로 나눠주는 것으로는 요건을 못 채웁니다.** 실제 출시가 필요합니다.

#### 왜 원스토어인가

| | 원스토어 | Google Play |
|---|---|---|
| 등록비 | **무료** | $25 |
| 신규 개인 계정 테스트 의무 | **없음** | 테스터 12명 × **14일 연속** |
| 심사 시간 | **1~2시간** | 수일 |
| 현실적 총 소요 | **하루** | **3~5주** |

Google Play 는 2023-11-13 이후 만든 개인 계정에 *"테스터 12명이 14일 연속 참여"* 를 요구합니다.
팀이 5명이라 외부에서 7명을 더 모아야 하고, 그들이 2주를 버텨야 하며, 그 뒤 프로덕션 액세스
심사가 또 붙습니다. **대회 일정에 맞추기 어렵습니다.** iOS 는 $99/년이 매년 나가 더 불리합니다.

#### 코드에서 막혀 있는 것

**릴리스 서명은 해결됐습니다**([#163](../../pull/163)). 예전에는 `release` 가
`signingConfigs.getByName("debug")` 를 써서 디버그 키로 서명됐고, 그건 스토어에 못 올립니다.

지금은 PKCS12 키스토어로 서명하고, CI 가 **서명 주체(`apksigner`)와 APK 안에 구워진 서버 주소까지**
검사합니다. 원스토어는 APK·AAB 둘 다 받으므로 APK 로 갑니다.

> ⚠️ 키스토어를 잃어버리면 앱을 영영 업데이트할 수 없습니다. **백업이 담당자 PC 한 곳뿐**이라
> 아직 위험이 남아 있습니다.

> ⚠️ `applicationId`(`com.fogapp.fogapp`)는 한 번 정하면 못 바꿉니다 — 확정 필요.

#### 🔴 서버가 24시간 떠 있어야 합니다

**해결됐습니다**([#154](../../issues/154)). 심사위원이 각자 시간에 설치해 써 보므로 담당자 PC
의존을 없애야 했습니다.

**네이버 클라우드**로 갔습니다 — 처음 후보였던 Oracle Cloud Always Free 는 **체크카드로 가입이
거부**됐습니다(신용카드 요구). 네이버 클라우드는 국내 PG 라 체크카드가 통과했고, 신규가입 크레딧
10만원으로 대회 기간을 덮습니다.

> ⚠️ **크레딧이 떨어지면 카드로 자동 과금됩니다.** 심사가 끝나면 **서버와 공인 IP 를 각각 반납**
> 해야 합니다 — 서버만 지우고 IP 를 남기면 요금이 계속 나갑니다.

> ⚠️ **서버 주소가 바뀌면 앱을 다시 빌드해 스토어에 올려야 합니다**(`API_BASE_URL` 이 빌드
> 타임에 구워짐). **스토어 등록 전에 최종 주소를 확정할 것.**

#### 🔑 대회 제출용 테스트 계정

심사자료가 계정 형식을 **지정**했습니다. 우리 앱은 이메일 로그인이므로 이메일 형식을 씁니다.

```
ID: openapi@메일도메인    PW: 2026openapi!
```

> ⚠️ 심사위원이 이 계정으로 **실제로 돌려봅니다.** 로그인 → 지도 → 스팟 인증까지 되는지 미리
> 확인하고, 발자취 잔여 횟수(기본 3)도 봐 두세요.

#### iOS 를 뺀 흔적

CI 의 `iOS Build`·`iOS Unsigned IPA` 잡과 iOS 설치 문서를 제거했습니다. Flutter 코드는 그대로라
되살리려면 git 이력에서 복원하면 됩니다. `app/ios/` 스캐폴딩은 남겨 둡니다.

---

## 🔗 단계 간 의존 관계 (동시 진행 팁)

- **Phase 0**은 반드시 먼저. 이후 **Phase 1(데이터)과 Phase 2(지도)는 상당 부분 병렬** 가능
  (지도팀은 더미 좌표로 먼저 시작 → API 준비되면 실 데이터로 교체).
- **🟪 소셜 트랙(SOC)은 Phase 1부터 별도 병렬**로 진행 — 지도 의존성 없는 로직·API·설계를 선행.
  (성향 설계 1-7 → 성향 UI·발자취 API 2-6·2-7 → 매칭 알고리즘 3-8)
- **Phase 3은 Phase 1·2가 모두 있어야** 완성됨(핵심 통합 지점).
- **Phase 4·5**는 지도(2·3)와 선행된 소셜 트랙을 **통합·UI 완성**하는 단계(대부분 로직은 이미 있음).
- **Phase 6**은 Phase 2(지도)·3(위치)·1(OpenAPI)에 의존.

```
탐험 코어 :        ┌──── Phase 1 (데이터) ────┐
        Phase 0 ─┤                            ├─ Phase 3 ─┬─ Phase 4 ─┐
                 └──── Phase 2 (지도) ────────┘           ├─ Phase 5 ─┼─ Phase 7
                                              └ Phase 6 ──┘
소셜 트랙 :        성향설계(1-7) ─ 성향·발자취(2-6·2-7) ─ 매칭알고리즘(3-8) ──┘
                  └───────────── SOC 김규현, Phase 1부터 병렬 ──────────────┘
```

---

## ✅ 마일스톤 요약

| 마일스톤 | 완료 시 상태 | 현재 | 태그 |
|----------|--------------|------|------|
| M0 | 모두가 개발 시작 가능 | ✅ | — |
| M1 | 로그인 + 스팟 데이터 확보 · 🟪 성향 점수 모델 확정 | ✅ | `v0.1.0` 에 포함 |
| M2 | 안개 지도 + 내 위치 표시 · 🟪 성향 테스트·발자취 API 완성 | ✅ | `v0.1.0` 에 포함 |
| **M3** | **탐험 핵심 루프 동작 (MVP)** · 🟪 매칭 알고리즘 동작 | ✅ | [`v0.1.0`](../../pull/92) |
| M4 | 발자취 지도 통합·완성 | ✅ | [`v0.1.0`](../../pull/92) |
| M5 | 매칭 화면·동행 성사 완성 | ✅ | [`v0.2.0`](../../pull/105) |
| **M5.5** | **발자취 = 길목의 글귀로 재설계** | ✅ **완료** | `v0.5.0` 예정 |
| M6 | 실시간 위치 공유 | ⏸ **마감 후로 연기** | — |
| **M7** | **출품 완성본 — 스토어 등록 링크 + 발표 자료** | 🔴 **지금 전부** | `v1.0.0` |

> 🏷️ **다음은 `v0.5.0`** — Phase 5.5 완결분(`dev` 의 [#127](../../pull/127)·[#129](../../pull/129)·[#130](../../pull/130)·[#137](../../pull/137))을 `main` 으로 올립니다.
> **원스토어에 올릴 릴리스 빌드는 이 태그에서 나옵니다.**
> 릴리스 PR(`dev → main`)은 **반드시 "Create a merge commit"** 으로 병합하세요.

---

> 이 계획은 고정이 아닙니다. 각 Phase 시작 전 팀 회의에서 범위를 조정하고,
> 세부 항목은 이슈로 분해해 담당자에게 할당하세요. 함께 안개를 걷어냅시다 🌫️➡️🗺️
