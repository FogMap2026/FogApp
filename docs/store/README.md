# 스토어 등록 자료

[docs/ONESTORE_RELEASE.md](../ONESTORE_RELEASE.md) 체크리스트 **⑧ 스토어 등록 자료**에 쓰는 이미지입니다.

| 파일 | 규격 | 용도 | 상태 |
|------|------|------|------|
| [icon-512.png](icon-512.png) | 512 × 512 | 대표 아이콘 | ✅ |
| [graphic-1024x578.png](graphic-1024x578.png) | 1024 × 578 | 그래픽 이미지 | ✅ |
| 스크린샷 | 2 ~ 8장 | 스토어 상세 | 🔶 **5/5 저장소에 있음** — 2장에 손볼 것이 남았습니다(아래) |

두 이미지 모두 **알파 채널 없는 RGB**입니다 — 스토어가 투명 배경을 거부하는 경우가 있어 맞춰 뒀습니다.

### 🔴 올리기 전에 남은 2건

| # | 무엇 | 다시 찍어야 하나 |
|---|---|---|
| 1 | **지도 컨트롤 위치가 바뀝니다** — [#187](../../pull/187)에서 폭 고정, [#190](../../pull/190)에서 우측 상단으로 이동. 지금 파일은 컨트롤이 **하단에 좌하단 버튼을 덮은 채** 찍혀 있습니다 | ⭕ APK 재빌드 후 |
| 5 | **축소본입니다** — 648 × 1342, 36 KB. 나머지는 1080 × 2237 | ❌ 단말 원본을 다시 옮기면 됩니다 |

> 📁 촬영 중 함께 찍힌 보조 컷(방문 인증 사진 확인, 빈 스팟 상세, 발자취 입력 중 키보드
> 화면)은 [extra/](extra/)에 따로 뒀습니다 — 스토어에 올릴 5장을 고르고 남은 것으로,
> 필요하면 여기서 골라 6~8번으로 추가할 수 있습니다.

## 스크린샷 — 5장 전부 찍었고 저장소에 들어갔습니다

`API_BASE_URL`이 빌드 타임에 APK 안으로 구워지므로([ONESTORE_RELEASE.md](../ONESTORE_RELEASE.md)) **주소가 확정되기 전에 찍은 화면은 곧 죽을 주소를 가리키는 앱**의 화면이었습니다. 그 선행 조건은 풀렸고, 실기기(인천 부평역)에서 루프를 한 번 돌며 찍었습니다.

| # | 화면 | 파일 |
|---|------|------|
| 1 | **안개 덮인 지도** — 이 앱이 무엇인지 한 장으로 | [screenshot-1.jpg](screenshot-1.jpg) — 인천 부평역 일대, 정복률 0% |
| 2 | **스팟 근접 알림 + 방문 인증** — 핵심 루프 | [screenshot-2.jpg](screenshot-2.jpg) — "부평모두몰 근처예요 (95m) — 지금 인증할 수 있어요" |
| 3 | **안개가 걷힌 뒤 정복률** — 보상 | [screenshot-3.jpg](screenshot-3.jpg) — 원형으로 걷힌 안개 + 정복률 3% |
| 4 | **발자취** — 길 위의 글귀 | [screenshot-4.jpg](screenshot-4.jpg) — 작성 완료 화면 |
| 5 | **동행 추천** — 성향 매칭 | [screenshot-5.jpg](screenshot-5.jpg) — 해질녘·골목길·안개를걷는사람 |

> 📌 **4번(발자취) 화면의 작성자 이름이 「이름 없는 여행자」로 뜹니다.** 촬영에 쓴 계정에
> 닉네임이 없어서입니다 — 5번에서 같은 문제가 없었던 건 [@sijin2170](https://github.com/sijin2170)
> 님이 본인 계정에 닉네임을 설정해 둔 덕분이었습니다. 이번엔 **그대로 쓰기로 했습니다** —
> 실제 사용자 경험을 보여주는 것도 자연스럽다는 판단입니다. 바꾸고 싶으면 그 계정에
> 닉네임을 설정하고 다시 찍으면 됩니다.

5번은 오랫동안 **찍는 사람 문제가 아니었습니다.** 서버·빌드 쪽 선행 조건이 둘 다 풀린 뒤([#176](../../pull/176) 반영 APK 재배포, [#179](../../pull/179) 상대 계정 시딩) 촬영했습니다.

> 📌 **찍고 나니 「이름 없는 여행자」 카드는 안 떴습니다** — 해질녘·골목길·안개를걷는사람 세 후보
> 모두 닉네임이 있는 상태로 나왔습니다. 그 문제로 걱정했던 카드는 목록에 없었거나
> ([@sijin2170](https://github.com/sijin2170) 계정이 그새 닉네임을 설정) 애초에 본인 계정이라
> 제외된 것으로 보입니다 — 스토어에 올릴 화면으로 그대로 쓸 수 있습니다.

> ⚠️ **재설치가 필요했습니다.** 폰에 [#163](../../pull/163) 이전(디버그 서명) APK 가 이미 깔려 있으면,
> 같은 파일을 다시 받아도 **서명 방식이 달라져 덮어설치되지 않고 옛 버전이 계속 실행**됩니다.
> 다음에 다시 찍을 일이 있으면 **기존 앱을 삭제하고 새로 설치**하세요 — 로그인이 된다고
> 최신 빌드라는 뜻은 아닙니다(로그인은 `API_BASE_URL` 만 검증합니다, [#180](../../pull/180)).

> ⚠️ **찍은 파일을 메신저로 옮기지 마세요.** 카톡·라인 등은 「일반 화질」로 보내면 이미지를
> **축소·재압축**합니다. 실제로 5번이 그렇게 들어와 1080 × 2237 대신 **648 × 1342 / 36 KB** 가
> 됐습니다. 다섯 장이 스토어에 나란히 붙는데 하나만 흐리면 그게 더 눈에 띕니다.
>
> **USB · 구글 포토 「원본」 다운로드 · 이메일 원본 첨부** 중 하나로 옮기세요. 넣은 뒤에
> 확인하려면 이렇게 잽니다.
>
> ```bash
> # JPEG SOF 마커에서 해상도를 읽는다 (ImageMagick 없이)
> for f in docs/store/*.jpg docs/store/extra/*.jpg; do
>   d=$(od -An -tx1 -N 4000 "$f" | tr -d ' \n' \
>       | grep -oE 'ffc[02]00[0-9a-f]{2}[0-9a-f]{2}[0-9a-f]{8}' | head -1)
>   printf '%-42s %s x %s\n' "$f" "$((16#${d:14:4}))" "$((16#${d:10:4}))"
> done
> ```
>
> **다섯 장이 다 `1080 x 2237` 이면 정상입니다.**

> ⚠️ **찍기 전에 APK 의 빌드 시각을 확인하세요.**
>
> 예전에 여기 *"로그인이 되면 맞습니다"* 라고 적혀 있었는데, **그것만으로는 부족합니다.** 로그인은
> 주소만 검증합니다. 주소가 맞아도 **화면을 고친 커밋이 안 들어간 APK** 로 찍으면, 고쳐진 줄 알았던
> 버그가 스토어 이미지에 그대로 남습니다(실제로 5번에서 이 일이 났습니다).
>
> ```bash
> curl -sI https://fogapp.taild500a1.ts.net/fogapp.apk | grep Last-Modified
> ```
>
> 이 시각이 **마지막 앱 커밋보다 뒤**인지 보세요. 아니면 재배포를 요청하고 기다리세요.
>
> 지금 올라가 있는 것은 **09-09 10:35 배포본**이고 [#171](../../pull/171)·[#163](../../pull/163)·[#176](../../pull/176)이 들어 있습니다. 그 뒤로 앱 커밋이
> 없다면 그대로 찍으셔도 됩니다.

**수집 범위는 전국입니다** — [#155](../../issues/155)로 **12,600 스팟 / 17개 시도**가 들어가 있어, 어디서 찍어도 주변에 스팟이 뜹니다(이전엔 서울·부산·제주 3개 지역뿐이었습니다).

> 💡 **심사 계정(`openapi@fogapp.kr`)으로 찍는 것이 좋습니다.** [#157](../../issues/157)로 서울 도심
> 이력이 심겨 있어, **안개가 걷힌 자리와 0%가 아닌 정복률**을 그대로 찍을 수 있습니다.
> 새 계정으로 찍으면 3·4번 화면을 만들려고 직접 걸어다녀야 합니다.
>
> | 지역 | 방문/전체 | 정복률 |
> |---|---|---|
> | 서울 종로구 | 8 / 137 | **5.8 %** |
> | 서울 중구 | 5 / 97 | **5.2 %** |
> | 인천 부평구 | 1 / 29 | **3.4 %** |
>
> 부평구는 시딩이 아니라 **실기기 테스트로 실제로 걸어서 생긴 기록**입니다.
>
> ⚠️ 다만 **인증 사진 파일은 없습니다**(DB의 `photo_url`만 심겨 있음). 인증 사진을 여는 화면은
> 피해서 찍으세요.

## 이미지를 다시 만들려면

`Pillow`만 있으면 됩니다. 색·문구를 바꿀 때 소스에서 고쳐 다시 뽑으세요 —
[tools/store_assets/](../../tools/store_assets/)에 생성 스크립트가 있습니다.

```bash
pip install Pillow

# 런처 아이콘(5개 밀도) + 스토어 512
python tools/store_assets/make_icon.py app/android/app/src/main/res docs/store/icon-512.png

# 그래픽 이미지 (스크립트 폴더에서 실행 — make_icon 을 import 합니다)
cd tools/store_assets && python make_graphic.py ../../docs/store/graphic-1024x578.png
```

> 그래픽 이미지는 한글 폰트로 **맑은 고딕**(`C:\Windows\Fonts\malgun*.ttf`)을 씁니다.
> Windows 밖에서 돌리려면 `make_graphic.py`의 `BOLD`·`REGULAR` 경로를 바꾸세요.

## 디자인 근거

안개(짙은 남색) 속에서 **한 곳만 걷혀 밝아지고 그 자리에 핀이 서 있는** 그림입니다. 이 앱이 하는 일 자체입니다.

앰버 `#F2B84B`는 임의로 고른 색이 아니라 **지도 위 발자취 도형과 같은 값**입니다(`footprint_marker_controller.dart`) — 스토어에서 본 색이 앱을 열었을 때 그대로 나옵니다.

런처 아이콘은 48px(mdpi)에서도 읽혀야 해서 요소를 **배경·걷힌 원·핀** 셋으로 제한했습니다.
